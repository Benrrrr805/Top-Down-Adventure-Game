import subprocess

# def test_entry_point():
#     # Use subprocess to execute the script as a standalone process
#     result = subprocess.run(
#         ["python", "main.py"],
#         capture_output=True,
#         text=True
#     )

#     # Verify that the script ran without errors
#     assert result.returncode == 0
